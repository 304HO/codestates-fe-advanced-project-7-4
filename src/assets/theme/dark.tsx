import { ThemeType } from "../../types/theme";

const theme: ThemeType = {
  id: "dark",
  primaryColor: "black",
  secondaryColor: "white",
  background: "white",
  color: "white"
};

export default theme;
